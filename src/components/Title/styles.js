import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    title: {
        fontSize: 24,
        fontWeight: "bold",
        color: "#EED2B8",
        fontFamily: "Roboto",
        textAlign: "center",
        marginTop: 20,
    },
});
export default styles;